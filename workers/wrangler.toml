name = "cnote-content-service"
main = "src/scraper.ts"
compatibility_date = "2024-12-01"

# Default configuration used by the Cloudflare Deploy Button. Each user deploys
# this Worker into their own Cloudflare account and receives their own workers.dev URL.
# Optional abuse protection:
# npx wrangler secret put CN_CONTENT_TOKEN
# SCRAPER_ALLOWED_ORIGINS can be configured in the Cloudflare dashboard.
