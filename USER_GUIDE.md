# Cnote 用户使用指南

## 目录
1. [快速开始](#快速开始)
2. [创建你的第一个 Flow](#创建你的第一个-flow)
3. [节点类型详解](#节点类型详解)
4. [AI 配置](#ai-配置)
5. [模板使用](#模板使用)
6. [常见问题](#常见问题)

---

## 快速开始

### 访问应用

**本地开发**:
```bash
cd web
npm install
npm run dev
```
访问 http://localhost:5173

**生产环境**: 
访问部署的 URL (待配置)

### 界面导览

Cnote 主界面包含以下页面:
- **Dashboard**: Flow 列表和管理
- **模板**: 预设工作流模板
- **内容库**: 管理你的内容素材
- **输出历史**: 查看所有生成的内容
- **设置**: API Keys 配置

---

## 创建你的第一个 Flow

### 示例: 创建一个文章总结工作流

#### 步骤 1: 创建新 Flow
1. 在 Dashboard 点击 "创建新 Flow"
2. 输入名称: "文章智能总结"
3. 点击进入 Flow 编辑器

#### 步骤 2: 添加内容输入节点
1. 从左侧节点面板拖拽 **Content** 节点到画布
2. 点击节点进入配置
3. 选择类型: **Text**
4. 输入或粘贴要总结的文章内容
5. 保存

#### 步骤 3: 添加 AI 处理节点
1. 拖拽 **AI** 节点到画布
2. 从 Content 节点的输出点拖动连线到 AI 节点的输入点
3. 点击 AI 节点配置:
   - **提供商**: OpenAI
   - **模型**: gpt-4
   - **系统提示**: "你是一个专业的内容总结助手"
   - **用户提示**: "请总结以下文章的核心要点，用 3-5 个要点列出"
4. 保存

#### 步骤 4: 添加输出节点
1. 拖拽 **Output** 节点到画布
2. 从 AI 节点拖动连线到 Output 节点
3. 点击 Output 节点配置:
   - **格式**: Markdown
   - **名称**: "文章总结结果"
4. 保存

#### 步骤 5: 执行 Flow
1. 确保已在设置中配置 OpenAI API Key
2. 点击顶部工具栏的 "执行" 按钮
3. 等待执行完成
4. 在 Output 节点或输出历史中查看结果

---

## 节点类型详解

### 1. Content 节点 (内容输入)

**用途**: 提供工作流的输入数据

**类型**:
- **Text**: 纯文本输入
- **YouTube**: YouTube 视频链接 (自动提取字幕)
- **Image**: 图片 URL
- **Video**: 视频 URL
- **Table**: 表格数据

**配置示例**:
```
类型: Text
内容: "这是我要处理的文本内容..."
```

**输出**: 配置的内容会传递给下游节点

---

### 2. AI 节点 (AI 处理)

**用途**: 使用 AI 模型处理内容

**支持的提供商**:
- OpenAI (GPT-4, GPT-3.5-turbo)
- Anthropic (Claude 3.5 Sonnet, Claude 3 Opus)
- DeepSeek (deepseek-chat)
- Google (Gemini Pro)
- xAI (Grok)
- Groq (LLaMA)
- OpenRouter (多种模型)

**配置项**:
- **提供商**: 选择 AI 服务商
- **模型**: 选择具体模型
- **系统提示**: 定义 AI 的角色和行为
- **用户提示**: 具体的处理指令
- **温度**: 0.0-2.0 (控制随机性)
- **最大 tokens**: 输出长度限制

**提示词技巧**:
```
系统提示: "你是一个专业的技术写作专家，擅长将复杂概念解释清楚。"

用户提示: "请将以下技术文档改写为适合初学者阅读的教程：

{input}

要求：
1. 使用简单易懂的语言
2. 添加实际例子
3. 分步骤说明"
```

**输入**: 从上游节点接收数据，可在提示词中用 `{input}` 引用

**输出**: AI 生成的文本内容

---

### 3. Browser 节点 (网页抓取)

**用途**: 抓取网页内容

**配置项**:
- **URL**: 要抓取的网页地址
- **选择器**: CSS 选择器 (可选，提取特定内容)

**配置示例**:
```
URL: https://example.com/article
选择器: article .content
```

**输出**: 抓取的网页文本内容

**限制**:
- 某些网站可能有反爬虫机制
- 内容长度限制: 50,000 字符

---

### 4. Output 节点 (输出)

**用途**: 保存工作流的最终结果

**格式选项**:
- **Text**: 纯文本
- **Markdown**: Markdown 格式
- **HTML**: HTML 格式

**配置项**:
- **格式**: 选择输出格式
- **名称**: 输出的标题 (可选)

**输出**: 内容会自动保存到"输出历史"

---

### 5. Editor 节点 (文本编辑)

**用途**: 手动编辑和格式化文本

**功能**:
- 富文本编辑 (基于 TipTap)
- 格式化: 粗体、斜体、标题、列表
- 链接插入
- 对齐方式

**使用场景**:
- 在 AI 生成后手动调整内容
- 组合多个输入源的内容
- 添加格式化和结构

---

### 6. PDF 节点 (PDF 处理)

**用途**: 处理 PDF 文件

**配置项**:
- **PDF URL**: PDF 文件链接
- **页码范围**: 例如 "1-10"

**输出**: 提取的 PDF 文本内容

---

### 7. Sticky 节点 (便签)

**用途**: 在画布上添加注释和说明

**功能**:
- 纯文本注释
- 不参与数据流
- 用于标记和组织 Flow

**使用场景**:
- 标注工作流的各个阶段
- 添加使用说明
- TODO 标记

---

### 8. Group 节点 (分组)

**用途**: 将相关节点分组

**功能**:
- 视觉组织
- 可以添加分组标题
- 可以折叠/展开

**使用场景**:
- 组织复杂的 Flow
- 逻辑分块
- 提高可读性

---

## AI 配置

### 添加 API Key

1. 点击右上角 "设置" → "API Keys"
2. 点击 "添加 API Key"
3. 填写信息:
   - **提供商**: 选择 AI 服务商
   - **API Key**: 输入你的密钥
   - **名称**: 自定义名称 (可选)
4. 点击保存

**安全说明**:
- API Key 使用 XOR 加密存储在本地 IndexedDB
- 数据不会上传到服务器
- 只在调用 AI 时通过 HTTPS 传输

### 获取 API Key

**OpenAI**:
1. 访问 https://platform.openai.com/api-keys
2. 登录账号
3. 点击 "Create new secret key"
4. 复制密钥

**Anthropic (Claude)**:
1. 访问 https://console.anthropic.com/
2. 登录账号
3. 进入 "API Keys" 页面
4. 创建新密钥

**其他提供商**: 参考各自官方文档

### CORS 代理配置 (可选)

Cnote 会自动检测是否需要使用 CORS 代理:
- 如果直连成功，自动使用直连
- 如果遇到 CORS 错误，自动切换到代理

**手动配置代理**:
1. 部署 Cloudflare Workers (见部署文档)
2. 在 `web/.env` 中配置:
```env
VITE_PROXY_URL=https://api.cnote.app
```
3. 重新构建应用

---

## 模板使用

### 使用预设模板

1. 点击顶部导航 "模板"
2. 浏览可用模板:
   - **简单文本处理**: 基础文本处理工作流
   - **YouTube 视频总结**: 提取字幕并生成总结
   - **网页内容分析**: 抓取并分析网页内容
3. 点击 "使用模板"
4. 系统会创建一个新 Flow 并复制模板的节点和连线
5. 根据需要修改配置

### 创建自己的模板

1. 在 Flow 编辑器中创建工作流
2. 点击 "保存为模板"
3. 填写信息:
   - **标题**: 模板名称
   - **描述**: 模板用途说明
   - **分类**: 自定义分类
4. 保存

**模板分享**: 未来版本将支持导出和分享模板

---

## 高级技巧

### 1. 多步骤 AI 处理

创建多个 AI 节点串联:
```
Content → AI (总结) → AI (翻译) → AI (润色) → Output
```

每个 AI 节点处理前一个节点的输出

### 2. 并行处理 (计划中)

未来版本将支持:
```
           ┌→ AI (总结) ──┐
Content ───┼→ AI (翻译) ──┼→ Group → Output
           └→ AI (关键词)─┘
```

### 3. 条件分支 (计划中)

未来版本将支持条件判断和分支

### 4. 撤销/重做

- **撤销**: Ctrl/Cmd + Z
- **重做**: Ctrl/Cmd + Shift + Z
- 历史记录: 最多 50 步

### 5. 画布操作

- **缩放**: 鼠标滚轮 或 触控板手势
- **平移**: 空格 + 拖拽 或 中键拖拽
- **适配视图**: 点击工具栏 "Fit View" 按钮
- **锁定画布**: 点击工具栏锁定按钮

### 6. 导入/导出

**导出 Flow**:
1. 在 Flow 编辑器点击 "导出"
2. 下载 JSON 文件

**导入 Flow**:
1. 在 Dashboard 点击 "导入"
2. 选择 JSON 文件
3. Flow 会被添加到列表

---

## 常见问题

### Q1: API Key 安全吗？
**A**: API Key 使用 XOR 加密存储在本地浏览器的 IndexedDB 中，不会上传到任何服务器。只在调用 AI API 时通过 HTTPS 加密传输。

### Q2: 为什么需要 Cloudflare Workers？
**A**: 由于浏览器的 CORS 限制，有些 AI API 不能直接从浏览器调用。Cloudflare Workers 作为代理解决这个问题。应用会自动检测并在需要时使用代理。

### Q3: 数据存储在哪里？
**A**: 所有数据 (Flows、模板、内容源、输出历史) 都存储在本地浏览器的 IndexedDB 中。如果 IndexedDB 不可用，会降级到 localStorage。

### Q4: 离线可以使用吗？
**A**: 应用本身可以离线使用，但调用 AI API 需要网络连接。

### Q5: 支持哪些浏览器？
**A**: 支持所有现代浏览器:
- Chrome/Edge 90+
- Firefox 88+
- Safari 14+

### Q6: Flow 执行失败怎么办？
**A**: 检查以下几点:
1. API Key 是否正确配置
2. 网络连接是否正常
3. AI API 余额是否充足
4. 节点配置是否正确
5. 查看浏览器控制台的错误信息

### Q7: 可以处理多大的文本？
**A**: 
- 单个节点输入: 建议 < 10,000 字
- AI 模型限制: 取决于具体模型 (通常 4k-128k tokens)
- 网页抓取: 限制 50,000 字符

### Q8: 如何删除所有数据？
**A**: 在浏览器设置中清除站点数据，或在开发者工具中删除 IndexedDB 数据库。

### Q9: 支持手机访问吗？
**A**: 当前版本针对桌面优化。移动端适配将在未来版本提供。

### Q10: 可以导出为 PDF 吗？
**A**: 当前支持导出为 Text/Markdown/HTML。可以使用第三方工具转换为 PDF。

---

## 故障排查

### 问题: Flow 执行卡住

**解决方法**:
1. 刷新页面
2. 检查是否有循环依赖 (节点互相引用)
3. 检查网络请求是否超时

### 问题: API 调用失败

**可能原因**:
- API Key 无效或过期
- API 余额不足
- 网络连接问题
- CORS 问题

**解决方法**:
1. 重新配置 API Key
2. 检查 API 账号余额
3. 确认网络连接
4. 部署 Cloudflare Workers 代理

### 问题: 浏览器性能问题

**优化建议**:
1. 减少画布上的节点数量 (< 50 个)
2. 关闭不必要的浏览器标签
3. 使用最新版本浏览器
4. 清理浏览器缓存

---

## 快捷键

| 快捷键 | 功能 |
|--------|------|
| Ctrl/Cmd + Z | 撤销 |
| Ctrl/Cmd + Shift + Z | 重做 |
| 空格 + 拖拽 | 平移画布 |
| 滚轮 | 缩放画布 |
| Delete/Backspace | 删除选中节点/边 |
| Ctrl/Cmd + S | 保存 (自动保存已启用) |

---

## 最佳实践

### 1. 组织 Flow
- 使用 Sticky 节点添加注释
- 用 Group 节点组织相关节点
- 保持清晰的从左到右或从上到下的数据流

### 2. 命名规范
- Flow 名称清晰描述用途
- 节点名称简洁明了
- 输出使用有意义的名称

### 3. 提示词编写
- 系统提示: 定义角色和总体要求
- 用户提示: 具体、清晰的指令
- 提供示例和格式要求
- 使用 `{input}` 引用上游数据

### 4. 模板管理
- 将常用工作流保存为模板
- 定期整理和更新模板
- 为模板添加详细描述

### 5. 性能优化
- 避免过长的提示词
- 合理设置 max tokens
- 不要在一个 Flow 中放太多节点

---

## 获取帮助

- **GitHub Issues**: https://github.com/lhc1119692022/Cnote/issues
- **文档**: 本指南和项目 README
- **示例**: 查看预设模板了解最佳实践

---

**Cnote - 让 AI 工作流更简单** 🚀
