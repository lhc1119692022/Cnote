# Cnote 用户使用指南

## 目录
1. [快速开始](#快速开始)
2. [创建你的第一个 Flow](#创建你的第一个-flow)
3. [节点类型详解](#节点类型详解)
4. [AI 配置](#ai-配置)
5. [模板使用](#模板使用)
6. [常见问题](#常见问题)

---

## 快速开始

### 访问应用

**本地开发**:
```bash
cd web
npm install
npm run dev
```
访问 http://localhost:5173

**生产环境**: 
访问部署的 URL (待配置)

### 界面导览

Cnote 主界面包含以下页面:
- **控制台**: Flow 列表、分组、备份和恢复
- **模板**: 预设工作流模板
- **内容**: 管理收藏的内容素材
- **设置**: 配置 AI 渠道、内容解析服务和本地存储

当前版本使用中文界面；需要其他语言时可使用浏览器自带的页面翻译。

---

## 创建你的第一个 Flow

### 示例: 创建一个文章总结工作流

#### 步骤 1: 创建新 Flow
1. 在控制台点击“创建 Flow”
2. 输入名称: "文章智能总结"
3. 点击进入 Flow 编辑器

#### 步骤 2: 添加内容输入节点
1. 点击画布顶部“添加”，选择“添加内容节点”
2. 在节点中选择“文本”
3. 输入或粘贴要总结的文章内容

#### 步骤 3: 添加 AI 处理节点
1. 点击画布顶部“AI 节点”
2. 从内容节点右侧连接点拖动到 AI 节点左侧连接点
3. 在 AI 节点选择已配置的模型
4. 输入“请总结上游文章的核心要点，用 3-5 个要点列出”并发送
5. AI 回复会保留在该节点的会话中，可复制、分支或转为文本节点继续编辑

---

## 节点类型详解

### 1. 内容节点

**用途**: 提供工作流的输入数据

**类型**:
- **文本**: 纯文本和 Markdown
- **视频**: YouTube、Bilibili、Vimeo、播客、远程或本地视频
- **社媒**: 支持的公开社媒页面
- **图片**: 图片 URL、粘贴图片或本地文件
- **文档**: PDF、DOCX 和网页文档
- **思维导图**: Markdown、XMind 等支持的格式
- **演示文稿**: PPT/PPTX 和支持的在线演示文稿
- **数据**: CSV、XLSX 和支持的在线表格

**配置示例**:
```
类型: Text
内容: "这是我要处理的文本内容..."
```

**输出**: 配置的内容会传递给下游节点

---

### 2. AI 节点

**用途**: 使用 AI 模型处理内容

**渠道**: 可配置 OpenAI、Anthropic、DeepSeek、Google Gemini、xAI、Groq、
OpenRouter 及兼容 Responses、Messages 或 Chat Completions 协议的自定义服务。

**配置项**:
- **渠道与模型**: 在设置中添加渠道并拉取或手动填写模型 ID
- **系统提示**: 定义 AI 的角色和行为
- **用户提示**: 具体的处理指令，可插入上游变量
- **联网与推理**: 仅在当前提供商、协议和模型明确支持时启用

**提示词技巧**:
```
系统提示: "你是一个专业的技术写作专家，擅长将复杂概念解释清楚。"

用户提示: "请将上游技术文档改写为适合初学者阅读的教程：

要求：
1. 使用简单易懂的语言
2. 添加实际例子
3. 分步骤说明"
```

**输入**: 从上游节点接收数据，可点击或拖入 AI 节点旁的变量胶囊引用

**输出**: AI 生成的文本内容

---

### 3. 浏览器节点

**用途**: 浏览网页，并向下游传递已确认的 URL、页面文本或两者

**配置项**:
- **URL**: 要打开的网页地址
- **传递模式**: URL、文本或 URL + 文本
- 页面地址发生变化后需要点击“更新”确认，避免把意外跳转传到下游

**配置示例**:
```
URL: https://example.com/article
传递模式: URL + 文本
```

**输出**: 抓取的网页文本内容

**限制**: 某些网站会拒绝嵌入或自动提取；此时仍可传递已确认的 URL。

---

### 4. 便签节点

**用途**: 在画布上添加注释和说明

**功能**:
- 纯文本注释
- 不参与数据流
- 用于标记和组织 Flow

**使用场景**:
- 标注工作流的各个阶段
- 添加使用说明
- TODO 标记

---

### 5. 分组节点

**用途**: 将相关节点分组

**功能**:
- 视觉组织
- 可以添加分组标题
- 可以折叠/展开

**使用场景**:
- 组织复杂的 Flow
- 逻辑分块
- 提高可读性

---

## AI 配置

### 添加 API Key

1. 打开“设置 → 渠道”
2. 点击“新增渠道”
3. 填写信息:
   - **渠道名称和提供商 ID**
   - **API 协议与接口地址**
   - **API Key**: 输入你的密钥
   - **模型**: 从接口拉取或手动添加模型 ID
4. 点击保存

**安全说明**:
- API Key 经 XOR 混淆后存储在本地 IndexedDB；这只能避免明文直读，不是安全加密
- 能访问当前浏览器数据或运行页面脚本的人仍可能取得 API Key
- 数据不会上传到服务器
- 只在调用 AI 时发送到你配置的官方接口或自建代理，请始终使用 HTTPS

### 获取 API Key

**OpenAI**:
1. 访问 https://platform.openai.com/api-keys
2. 登录账号
3. 点击 "Create new secret key"
4. 复制密钥

**Anthropic (Claude)**:
1. 访问 https://console.anthropic.com/
2. 登录账号
3. 进入 "API Keys" 页面
4. 创建新密钥

**其他提供商**: 参考各自官方文档

### 跨域接口（可选）

如果渠道显示“第三方 API 跨域错误！”，请展开下方提示并打开 AI 代理部署
文档。一个 Worker 可以配置多条第三方线路；部署后打开 Worker 根地址，
复制页面中已经拼好的 `Worker 地址/proxy/线路名` 填回对应渠道。

---

## 模板使用

### 使用预设模板

1. 点击顶部导航 "模板"
2. 浏览可用模板:
   - **简单文本处理**: 基础文本处理工作流
   - **YouTube 视频总结**: 提取字幕并生成总结
   - **网页内容分析**: 抓取并分析网页内容
3. 点击 "使用模板"
4. 系统会创建一个新 Flow 并复制模板的节点和连线
5. 根据需要修改配置

### 创建自己的模板

1. 在 Flow 编辑器中创建工作流
2. 点击 "保存为模板"
3. 填写信息:
   - **标题**: 模板名称
   - **描述**: 模板用途说明
   - **分类**: 自定义分类
4. 保存

**模板分享**: 未来版本将支持导出和分享模板

---

## 高级技巧

### 1. 多步骤 AI 处理

创建多个 AI 节点串联:
```
内容 → AI (总结) → AI (翻译) → AI (润色)
```

每个 AI 节点处理前一个节点的输出

### 2. 并行处理

同一个内容节点可以连接多个下游 AI 节点:
```
内容 ───┬→ AI (总结)
        ├→ AI (翻译)
        └→ AI (关键词)
```

### 3. 条件分支 (计划中)

未来版本将支持条件判断和分支

### 4. 撤销/重做

- **撤销**: Ctrl/Cmd + Z
- **重做**: Ctrl/Cmd + Shift + Z
- 历史记录: 最多 50 步

### 5. 画布操作

- **缩放**: 鼠标滚轮 或 触控板手势
- **平移**: 空格 + 拖拽 或 中键拖拽
- **适配视图**: 点击工具栏 "Fit View" 按钮
- **锁定画布**: 点击工具栏锁定按钮

### 6. 导入/导出

**导出工作流 JSON 或画布 PNG**:
1. 在 Flow 编辑器点击 "导出"
2. 选择“导出工作流 JSON”或“将画布导出为 PNG”
3. JSON 不包含画布引用的本地文件，适合分享工作流结构

**完整 ZIP 备份**:
1. 在 Dashboard 打开 Flow 卡片右上角菜单
2. 点击“备份”，下载 `.cnote.zip` 文件
3. ZIP 同时包含工作流和画布引用的本地资源；单个资源超过 500 MiB 时需手动备份

**恢复完整备份**:
1. 在 Dashboard 点击“恢复备份”
2. 选择 `.cnote.zip` 文件
3. Flow 和备份中的本地资源会一并恢复

**导入工作流 JSON**:
1. 在 Flow 编辑器点击“导入”
2. 选择 JSON 文件
3. 当前 Flow 的工作流结构会被替换

---

## 常见问题

### Q1: API Key 安全吗？
**A**: API Key 经 XOR 混淆后存储在当前浏览器的 IndexedDB 中，不会由 Cnote 上传到公共服务器。XOR 不是安全加密，不能防止能访问浏览器数据或执行页面脚本的人读取密钥。调用 AI 时，密钥会发送到你配置的官方接口或自建代理。

### Q2: 为什么需要 Cloudflare Workers？
**A**: 内容解析 Worker 用于网页正文、字幕和公开社媒内容解析；AI CORS 代理只在服务商不允许浏览器直连时使用。两者都由用户按需自行部署，Cnote 不会自动把 API Key 发送到公共代理。

### Q3: 数据存储在哪里？
**A**: Flows、模板、内容源、AI 渠道和本地资源都保存在当前站点的浏览器存储中。没有账号云同步，请定期使用完整 ZIP 备份保存重要 Flow。

### Q4: 离线可以使用吗？
**A**: 应用本身可以离线使用，但调用 AI API 需要网络连接。

### Q5: 支持哪些浏览器？
**A**: 支持所有现代浏览器:
- Chrome/Edge 90+
- Firefox 88+
- Safari 14+

### Q6: Flow 执行失败怎么办？
**A**: 检查以下几点:
1. API Key 是否正确配置
2. 网络连接是否正常
3. AI API 余额是否充足
4. 节点配置是否正确
5. 查看浏览器控制台的错误信息

### Q7: 可以处理多大的文本？
**A**: 
- 单个节点输入: 建议 < 10,000 字
- AI 模型限制: 取决于具体模型 (通常 4k-128k tokens)
- 网页抓取: 限制 50,000 字符

### Q8: 如何删除所有数据？
**A**: 在浏览器设置中清除站点数据，或在开发者工具中删除 IndexedDB 数据库。

### Q9: 支持手机访问吗？
**A**: 当前版本针对桌面优化。移动端适配将在未来版本提供。

### Q10: 可以导出哪些格式？
**A**: 画板可导出工作流 JSON 或 PNG；控制台可创建包含本地资源的 `.cnote.zip` 完整备份。文本和图片内容节点还可通过节点顶部的下载按钮另存到本地。

---

## 故障排查

### 问题: Flow 执行卡住

**解决方法**:
1. 刷新页面
2. 检查是否有循环依赖 (节点互相引用)
3. 检查网络请求是否超时

### 问题: API 调用失败

**可能原因**:
- API Key 无效或过期
- API 余额不足
- 网络连接问题
- CORS 问题

**解决方法**:
1. 重新配置 API Key
2. 检查 API 账号余额
3. 确认网络连接
4. 部署 Cloudflare Workers 代理

### 问题: 浏览器性能问题

**优化建议**:
1. 减少画布上的节点数量 (< 50 个)
2. 关闭不必要的浏览器标签
3. 使用最新版本浏览器
4. 清理浏览器缓存

---

## 快捷键

| 快捷键 | 功能 |
|--------|------|
| Ctrl/Cmd + Z | 撤销 |
| Ctrl/Cmd + Shift + Z | 重做 |
| 空格 + 拖拽 | 平移画布 |
| 滚轮 | 缩放画布 |
| Delete/Backspace | 删除选中节点/边 |
| Ctrl/Cmd + S | 保存 (自动保存已启用) |

---

## 最佳实践

### 1. 组织 Flow
- 使用 Sticky 节点添加注释
- 用 Group 节点组织相关节点
- 保持清晰的从左到右或从上到下的数据流

### 2. 命名规范
- Flow 名称清晰描述用途
- 节点名称简洁明了
- 输出使用有意义的名称

### 3. 提示词编写
- 系统提示: 定义角色和总体要求
- 用户提示: 具体、清晰的指令
- 提供示例和格式要求
- 使用 `{input}` 引用上游数据

### 4. 模板管理
- 将常用工作流保存为模板
- 定期整理和更新模板
- 为模板添加详细描述

### 5. 性能优化
- 避免过长的提示词
- 合理设置 max tokens
- 不要在一个 Flow 中放太多节点

---

## 获取帮助

- **GitHub Issues**: https://github.com/lhc1119692022/Cnote/issues
- **文档**: 本指南和项目 README
- **示例**: 查看预设模板了解最佳实践

---

**Cnote - 让 AI 工作流更简单** 🚀
